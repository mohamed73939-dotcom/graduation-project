# -------------------- 8. تشغيل الاختبارات --------------------
"""
# تشغيل جميع الاختبارات
pytest

# تشغيل اختبارات محددة
pytest tests/test_whisper_engine.py

# مع تغطية الكود
pytest --cov=backend --cov-report=html

# تشغيل الاختبارات السريعة فقط
pytest -m "not slow"

# تشغيل بشكل مفصل
pytest -vv

# إيقاف عند أول فشل
pytest -x

# إعادة تشغيل الاختبارات الفاشلة فقط
pytest --lf
"""